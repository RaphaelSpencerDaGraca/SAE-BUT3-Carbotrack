# =========================
# PostgreSQL (Docker)
# =========================
POSTGRES_USER=user
POSTGRES_PASSWORD=1234
POSTGRES_DB=carbotrack_db

# =========================
# Backend -> DB connection
# =========================
# When you run the backend on your machine (npm run dev)
DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=1234
DB_NAME=carbotrack_db

# When you run the backend inside Docker (docker compose up)
DB_HOST_DOCKER=db

# =========================
# Auth
# =========================
# Change this in your local .env (do NOT commit .env)
JWT_SECRET=change_me
